# Canonical scripts

This directory contains downstream canonicalization scripts for the PAM Observatory.

These scripts:

- consolidate existing study outputs into canonical artifacts,
- preserve provenance and provisionality explicitly,
- do not replace upstream OBS study scripts,
- and may later be promoted into `src/pam/` once contracts stabilize.

The initial focus is the family/gateway canonicalization layer.
