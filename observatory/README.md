# Observatory

This directory is the future canonical artifact root for the PAM instrument.

During migration, active runtime outputs may still be written to `outputs/`.
Later PRs will move artifact path ownership into the canonical pipeline and IO layer.
